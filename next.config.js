/** @type {import('next').NextConfig} */
const nextConfig = {
  // Required so Next.js doesn't try to serve its own server when using custom server.js
  // Custom server is used for Socket.io integration
};

module.exports = nextConfig;
